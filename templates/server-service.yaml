apiVersion: v1
kind: Service
metadata:
  name: {{ include "mvn-stack.backendFullname" . }}
spec:
  ports:
    - port: {{ .Values.server.service.port }}
      targetPort: {{ .Values.server.service.port }}
  selector:
    app: {{ include "mvn-stack.backendFullname" . }}
